/* 
  Importante: 
  No modificar ni el nombre ni los argumetos que reciben las funciones, sólo deben escribir
  código dentro de las funciones ya definidas. 
  No comentar la funcion 
*/
function doble(array) {
  // la funcion recibe un array de numeros enteros y debe devolver otro arreglo con el doble de cada valor
  // ej:
  //doble([1, 2, 3]) devuelve [2, 4, 6]
  // Tu código aca:
  


  //crear un arreglo que nos devuelve un array
  // iterar sobre el array
  //multiplicar el array con el numero 2
  //retornar el array multiplicado

  let doubles = [];

    for (let i = 0; i < array.length; i++)  {
      doubles[i] = doubles[i] * 2;
     
 

  //var numbers = [1,2,3];
  //var numbersTimes = numbers.map(function(num) {
    //return num * 2;
     
} return doubles * 2

// console.log(doble(numbers())

// No modifiques nada debajo de esta linea //

module.exports = doble